// react imports
import React from 'react';
// 3rd imports
// local imports
import { TabPanelProps } from './interface';

const TabPanel = (props: TabPanelProps) => {

    // ------------------------- || render function || -------------------------
    return <> Đây là tabPnale component   </>;
}

export default TabPanel;
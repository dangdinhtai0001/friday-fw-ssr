// react imports
import React from 'react';
// 3rd imports
// local imports
import { TabsProps } from './core/interface'

const Tabs = (props: TabsProps) => {

    // ------------------------- || render function || -------------------------
    return <>    </>;
}

export default Tabs;
/* eslint-disable no-unused-vars */
import * as React from 'react';
import { GridPopupProps } from '../Grid.d';

function GridPopup(props: GridPopupProps): React.ReactNode {
    return <></>;
}
export default GridPopup;
